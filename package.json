{
  "name": "root",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "lerna run build",
    "bootstrap": "lerna link && lerna bootstrap",
    "release": "lerna publish patch --force-publish --exact"
  },
  "devDependencies": {
    "lerna": "3.14.1"
  },
  "author": "",
  "license": "ISC",
  "version": "0.0.0"
}
